# =============================================================================
# IMAGE WORKFLOW — Visual System integration
# =============================================================================

ImageWorkflow:
  upload: via Visual System — NOT direct URL
  adminSelect: existing assets from Visual System
  futureGeneration:
    status: NOT implement now
    workflow: Admin prompt -> image generation -> asset draft -> review -> approve -> attach
  rights:
    metadata:
      items:
        - source
        - license
        - status

events:
  items:
    - ImageAssetAttached
