# =============================================================================
# ORGANIZATION CARDS — emblem, name, type, status
# =============================================================================

OrganizationCards:
  fields:
    items:
      - emblem
      - name
      - type
      - status

events:
  items:
    - OrganizationCardViewed
