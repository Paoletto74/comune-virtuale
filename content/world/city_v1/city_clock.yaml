# =============================================================================
# CITY CLOCK — ora, giorno, data from Time System
# =============================================================================

CityClock:
  displays:
    items:
      - ora
      - giorno
      - data
      - eventuale_stato
  sourceOfTruth: Time System — ref time_main_v1
  openingHours:
    rule: alcune location hanno opening hours — Business/Work/Location rules

events:
  items:
    - CityClockUpdated
