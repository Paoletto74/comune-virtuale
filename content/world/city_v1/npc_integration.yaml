# =============================================================================
# NPC INTEGRATION — NPC populates city, NPC != Citizen
# =============================================================================

NpcIntegration:
  rule: NPC System popola citta — NPC != Citizen
  associations:
    items:
      - locations
      - jobs
      - businesses
      - events
  visuals: NPC/Visual Systems — ref npc_main_v1
  ref: npc_main_v1

events:
  items:
    - NpcAtLocation
