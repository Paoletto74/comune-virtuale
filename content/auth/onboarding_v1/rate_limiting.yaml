# =============================================================================
# RATE LIMITING — auth endpoints
# =============================================================================

RateLimiting:
  applies: authentication endpoints
  ref: auth_main_v1/rate_limits.yaml

events:
  items:
    - RateLimitExceeded
