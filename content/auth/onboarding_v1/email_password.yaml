# =============================================================================
# EMAIL PASSWORD — registration, login, reset
# =============================================================================

EmailPassword:
  supports:
    items:
      - registration
      - login
      - password_validation
      - password_reset
      - email_verification
      - logout
  emailVerification: DA DEFINIRE / TBD — dipende da provider
  ref: auth_main_v1/password.yaml, recovery.yaml

events:
  items:
    - EmailRegistrationStarted
    - EmailLoginSucceeded
    - EmailLoginFailed
