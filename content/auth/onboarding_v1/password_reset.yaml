# =============================================================================
# PASSWORD RESET — forgot password workflow
# =============================================================================

PasswordReset:
  flow: Forgot password — workflow separato
  ref: auth_main_v1/recovery.yaml

events:
  items:
    - PasswordResetRequested
    - PasswordResetCompleted
