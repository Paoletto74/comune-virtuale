# =============================================================================
# VEHICLE — mobility_vehicles_v1
# =============================================================================

---
Vehicle:
  schema:
    vehicleId: string
    type: string
    ownerId: string
    locationId: string
    condition: string
    configuration: object
    valuation: object
    status: string
    visualAssetId: string
  schemaDefinitive: TBD
  statusLabel: PROPOSTA TECNICA
